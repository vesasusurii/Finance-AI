[pytest]
asyncio_mode = auto
testpaths = tests
norecursedirs = .git __pycache__ integration
markers =
    integration: requires Postgres and Redis (skipped in default CI unit job)
filterwarnings =
    ignore::pytest.PytestUnknownMarkWarning
